<snippet>
    <content><![CDATA[
switch (${1:var}) {
    case ${2:1}:
        ${3:// do something}
      break;
    case ${4:2}:
        ${5:// do something}
      break;
    default:
        ${6:// do something}
}
]]></content>
    <tabTrigger>switch()</tabTrigger>
    <scope>source.arduino</scope>
    <description>Arduino</description>
</snippet>
